# Normalise les fins de ligne en LF dans le dépôt, quelle que soit la plateforme locale
# (élimine les warnings CRLF/LF observés sous Windows).
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
